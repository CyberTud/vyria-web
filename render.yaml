services:
  - type: web
    name: vyria-backend
    env: node
    rootDir: server
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: ADMIN_PASSWORD
        value: tudorregele
      - key: NODE_VERSION
        value: 18.20.0